# V3 Polish — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Три точечных исправления после финального аудита V3.

**Architecture:** Всё уже реализовано — Reports empty state и ClientAdd 409 уже работают. Остались: timeout-сообщение в Broadcast, invalidation Reports после заказа, обновление CLAUDE.md.

**Tech Stack:** React 19, axios, @tanstack/react-query, CLAUDE.md markdown.

---

### Task 1: Broadcast — timeout error message

**Files:**
- Modify: `miniapp/src/master/pages/Broadcast.jsx` (строка ~617-623)

**Step 1: Заменить `onError` в `sendMutation`**

Найти:
```js
    onError: (err) => {
      if (typeof WebApp?.MainButton?.hideProgress === 'function') {
        WebApp.MainButton.hideProgress();
      }
      const msg = err?.response?.data?.detail || 'Ошибка отправки';
      alert(msg);
    },
```

Заменить на:
```js
    onError: (err) => {
      if (typeof WebApp?.MainButton?.hideProgress === 'function') {
        WebApp.MainButton.hideProgress();
      }
      const isTimeout = err?.code === 'ECONNABORTED' || (err?.response?.status >= 504);
      const msg = isTimeout
        ? 'Рассылка заняла больше времени — проверьте результат позже'
        : (err?.response?.data?.detail || 'Ошибка отправки');
      alert(msg);
    },
```

**Step 2: Проверить сборку**

```bash
cd /Users/evgenijpastusenko/Projects/Master_bot/miniapp
npm run build 2>&1 | tail -5
```

Ожидаемый вывод: `✓ built in` без ошибок.

**Step 3: Commit**

```bash
git add miniapp/src/master/pages/Broadcast.jsx
git commit -m "fix(broadcast): show timeout message on ECONNABORTED or 504"
```

---

### Task 2: MasterApp — invalidate Reports после обновления заказа

**Files:**
- Modify: `miniapp/src/master/MasterApp.jsx` (строка ~83-87)

**Step 1: Добавить инвалидацию в `invalidateOrders`**

Найти:
```js
  const invalidateOrders = () => {
    queryClient.invalidateQueries({ queryKey: ['master-dashboard'] });
    queryClient.invalidateQueries({ queryKey: ['master-calendar'] });
    queryClient.invalidateQueries({ queryKey: ['master-orders'] });
  };
```

Заменить на:
```js
  const invalidateOrders = () => {
    queryClient.invalidateQueries({ queryKey: ['master-dashboard'] });
    queryClient.invalidateQueries({ queryKey: ['master-calendar'] });
    queryClient.invalidateQueries({ queryKey: ['master-orders'] });
    queryClient.invalidateQueries({ queryKey: ['master-reports'] });
  };
```

**Step 2: Проверить сборку**

```bash
cd /Users/evgenijpastusenko/Projects/Master_bot/miniapp
npm run build 2>&1 | tail -5
```

Ожидаемый вывод: `✓ built in` без ошибок.

**Step 3: Commit**

```bash
git add miniapp/src/master/MasterApp.jsx
git commit -m "fix(reports): invalidate master-reports cache after order update"
```

---

### Task 3: CLAUDE.md — актуализация

**Files:**
- Modify: `CLAUDE.md`

**Step 1: Обновить описание Broadcast.jsx в структуре проекта**

Найти:
```
│           │   ├── Broadcast.jsx  — 3-шаговая рассылка (сегмент, текст, отправка)
```

Заменить на:
```
│           │   ├── Broadcast.jsx  — 4-шаговая рассылка (текст, медиа, сегмент, отправка)
```

**Step 2: Добавить новые файлы в структуру проекта**

В блок `miniapp/src/master/components/` найти последнюю запись и добавить после неё:
```
│           │   └── ClientAddSheet.jsx — bottom sheet добавления нового клиента
```

В блок `miniapp/src/master/pages/` найти строку с `Broadcast.jsx` и добавить после неё:
```
│           │   ├── Reports.jsx    — аналитика: выручка, заказы, клиенты, график
│           │   ├── ClientsList.jsx — список клиентов с поиском и пагинацией
│           │   └── ClientCard.jsx — карточка клиента: история, бонусы, редактирование
```

**Step 3: Обновить broadcast.py в структуре**

Найти:
```
│           └── broadcast.py   — POST /api/master/broadcast/preview, /send, /segments
```

Заменить на:
```
│           ├── broadcast.py   — POST /api/master/broadcast/preview (JSON), /send (multipart/form-data с опциональным UploadFile), /segments
│           └── reports.py     — GET /api/master/reports (параметры: period или date_from/date_to)
```

**Step 4: Обновить раздел "Реализовано" — добавить новые пункты**

Найти строку:
```
- **Master Mini App бэкенд** — `src/api/routers/master/`: dashboard, calendar, orders (CRUD), clients, services, broadcast, invite-link
```

Заменить на:
```
- **Master Mini App бэкенд** — `src/api/routers/master/`: dashboard, calendar, orders (CRUD), clients (GET list + POST create), services, broadcast, reports, invite-link
```

Найти строку:
```
  - Broadcast — 3-шаговая рассылка (сегмент, текст, предпросмотр и отправка)
```

Заменить на:
```
  - Broadcast — 4-шаговая рассылка (текст, медиа фото/видео, сегмент, предпросмотр и отправка); `/send` принимает `multipart/form-data`
  - Reports (Аналитика) — выручка, заказы, новые клиенты, топ услуг, график по дням; доступ через клики на StatCard Dashboard
  - ClientsList — список клиентов с поиском и бесконечной прокруткой
  - ClientCard — карточка клиента: история заказов, бонусы, редактирование
  - ClientAddSheet — bottom sheet добавления клиента; обрабатывает 409 (дубликат, архивный)
```

**Step 5: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(claude-md): actualize V3 — Broadcast 4-step, Reports, ClientsList, ClientAdd"
```

---

### Task 4: Deploy

**Step 1: Push и деплой фронтенда**

```bash
git push origin main
bash deploy_miniapp.sh
```

Ожидаемый вывод: `=== Done! === Mini App: https://app.crmfit.ru`
