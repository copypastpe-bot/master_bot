# Master CRM Bot — Полная спецификация проекта

**Версия:** 1.0 MVP  
**Дата:** 27.02.2026  
**Статус:** В разработке

---

## 1. Обзор проекта

SaaS-сервис для частных мастеров в сфере услуг (клининг, сантехника, электрика, ремонт техники, красота и др.).  
Позволяет вести базу клиентов, управлять заказами, запускать программу лояльности и делать маркетинговые рассылки — всё через Telegram.

### 1.1 Целевая аудитория

**Мастера** — частные специалисты, работающие на выезде к клиенту. Разные ниши, один инструмент.

**Клиенты мастеров** — физические лица, получающие услуги на дому.

### 1.2 Ключевые принципы

- **Простота** — минимум шагов для любого действия
- **Один экран** — Home как точка входа во все функции
- **Inline UI** — только inline-кнопки, сообщение редактируется (не плодим новые)
- **Без лишнего** — каждая функция должна оправдывать своё место

---

## 2. Архитектура

### 2.1 Компоненты

```
master-crm-bot/
├── master_bot.py          # Бот для мастеров
├── client_bot.py          # Бот для клиентов
├── database.py            # Слой работы с БД (абстракция SQLite/PostgreSQL)
├── models.py              # Dataclass-модели сущностей
├── scheduler.py           # Фоновые задачи (напоминания, ДР, рассылки)
├── google_calendar.py     # Интеграция с Google Calendar (бот → GC)
├── notifications.py       # Отправка уведомлений клиентам
├── keyboards.py           # Все inline-клавиатуры
├── states.py              # FSM состояния (aiogram)
├── config.py              # Конфигурация из .env
├── utils.py               # Вспомогательные функции
├── migrations/            # SQL миграции
│   ├── 001_init.sql
│   └── ...
├── .env.example
├── requirements.txt
├── README.md
└── SPEC.md                # Этот файл
```

### 2.2 Два бота, одна база данных

| Компонент | Назначение |
|---|---|
| `master_bot` | Мастера: управление клиентами, заказами, бонусами, маркетингом |
| `client_bot` | Клиенты: баланс бонусов, история, заявки, уведомления |
| `database.py` | Единый слой БД для обоих ботов |

### 2.3 База данных

**Dev:** SQLite (`db.sqlite3`)  
**Prod:** PostgreSQL на VPS

Слой БД абстрагируется через `database.py` — смена БД без изменения логики ботов.

### 2.4 Фоновые задачи (scheduler.py)

| Задача | Расписание |
|---|---|
| Напоминание клиенту за 24ч | Каждый час — проверка заказов |
| Напоминание клиенту за 1ч | Каждые 15 мин — проверка заказов |
| Бонус на день рождения | Ежедневно в 10:00 |
| Рассылка по неактивным | По расписанию мастера (v2) |

---

## 3. База данных — схема

### 3.1 Таблицы

```sql
-- Мастера
CREATE TABLE masters (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    tg_id           BIGINT UNIQUE NOT NULL,
    name            TEXT NOT NULL,
    sphere          TEXT,                    -- сфера деятельности
    socials         TEXT,                    -- соцсети (текст)
    contacts        TEXT,                    -- контакты (текст)
    work_hours      TEXT,                    -- режим работы (текст)
    invite_token    TEXT UNIQUE NOT NULL,    -- токен для инвайт-ссылки клиентов
    -- Бонусная программа
    bonus_enabled   BOOLEAN DEFAULT TRUE,
    bonus_rate      REAL DEFAULT 5.0,        -- % начисления от суммы заказа
    bonus_max_spend REAL DEFAULT 50.0,       -- макс. % суммы заказа, оплачиваемый бонусами
    bonus_birthday  INTEGER DEFAULT 300,     -- бонус на ДР (в рублях)
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Клиенты
CREATE TABLE clients (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    tg_id           BIGINT UNIQUE,           -- NULL если добавлен мастером вручную
    name            TEXT NOT NULL,
    phone           TEXT,
    birthday        DATE,
    registered_via  INTEGER REFERENCES masters(id), -- через какого мастера пришёл
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Связь мастер ↔ клиент
CREATE TABLE master_clients (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    master_id       INTEGER NOT NULL REFERENCES masters(id),
    client_id       INTEGER NOT NULL REFERENCES clients(id),
    bonus_balance   INTEGER DEFAULT 0,       -- текущий баланс бонусов (в рублях)
    total_spent     INTEGER DEFAULT 0,       -- всего потрачено у мастера
    note            TEXT,                    -- заметка мастера (приватная)
    first_visit     TIMESTAMP,
    last_visit      TIMESTAMP,
    UNIQUE(master_id, client_id)
);

-- Справочник услуг мастера
CREATE TABLE services (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    master_id       INTEGER NOT NULL REFERENCES masters(id),
    name            TEXT NOT NULL,
    price           INTEGER,                 -- цена в рублях (NULL = не указана)
    is_active       BOOLEAN DEFAULT TRUE,
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Заказы
CREATE TABLE orders (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    master_id       INTEGER NOT NULL REFERENCES masters(id),
    client_id       INTEGER NOT NULL REFERENCES clients(id),
    address         TEXT,
    scheduled_at    TIMESTAMP,               -- дата и время записи
    status          TEXT DEFAULT 'new',      -- new | confirmed | done | moved | cancelled
    payment_type    TEXT,                    -- cash | card | transfer | invoice
    amount_total    INTEGER,                 -- итоговая сумма (в рублях)
    bonus_accrued   INTEGER DEFAULT 0,       -- начислено бонусов
    bonus_spent     INTEGER DEFAULT 0,       -- списано бонусов
    cancel_reason   TEXT,
    gc_event_id     TEXT,                    -- Google Calendar event ID
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    done_at         TIMESTAMP
);

-- Позиции заказа (услуги)
CREATE TABLE order_items (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    order_id        INTEGER NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
    service_id      INTEGER REFERENCES services(id),  -- NULL если введено вручную
    name            TEXT NOT NULL,           -- название услуги (копия на момент заказа)
    price           INTEGER NOT NULL         -- цена на момент заказа
);

-- Лог бонусных операций
CREATE TABLE bonus_log (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    master_id       INTEGER NOT NULL REFERENCES masters(id),
    client_id       INTEGER NOT NULL REFERENCES clients(id),
    order_id        INTEGER REFERENCES orders(id),
    type            TEXT NOT NULL,           -- accrual | spend | manual | birthday | promo
    amount          INTEGER NOT NULL,        -- положительное = начисление, отрицательное = списание
    comment         TEXT,
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Маркетинговые кампании (акции и рассылки)
CREATE TABLE campaigns (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    master_id       INTEGER NOT NULL REFERENCES masters(id),
    type            TEXT NOT NULL,           -- promo | broadcast
    title           TEXT,                    -- для акций: название
    text            TEXT NOT NULL,
    active_from     DATE,                    -- для акций: начало
    active_to       DATE,                    -- для акций: конец
    segment         TEXT DEFAULT 'all',      -- all | inactive_3m | inactive_6m | new_30d
    sent_at         TIMESTAMP,
    sent_count      INTEGER DEFAULT 0,
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Входящие запросы от клиентов (вопросы, заявки, медиа)
CREATE TABLE inbound_requests (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    master_id       INTEGER NOT NULL REFERENCES masters(id),
    client_id       INTEGER NOT NULL REFERENCES clients(id),
    type            TEXT NOT NULL,           -- question | order_request | media
    text            TEXT,
    service_name    TEXT,                    -- для order_request
    file_id         TEXT,                    -- для media
    is_read         BOOLEAN DEFAULT FALSE,
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 3.2 Индексы

```sql
CREATE INDEX idx_orders_master_scheduled ON orders(master_id, scheduled_at);
CREATE INDEX idx_orders_master_status ON orders(master_id, status);
CREATE INDEX idx_master_clients_master ON master_clients(master_id);
CREATE INDEX idx_bonus_log_master_client ON bonus_log(master_id, client_id);
CREATE INDEX idx_clients_phone ON clients(phone);
CREATE INDEX idx_clients_tg_id ON clients(tg_id);
```

---

## 4. master_bot — Функциональные требования

### 4.1 Регистрация мастера

Триггер: `/start` от нового пользователя  
Сценарий (анкета, FSM):
1. Введите ваше имя / псевдоним
2. Укажите сферу деятельности (свободный текст)
3. Контакты для клиентов (телефон, мессенджеры)
4. Соцсети и каналы (ссылки)
5. Режим работы (текст, например "пн-пт 9:00-19:00")

После регистрации: генерируется `invite_token`, показывается Home экран.

### 4.2 Home экран

Сообщение редактируется, не создаётся новое.

```
👋 Привет, [Имя]!

📅 Сегодня, [дата]:
• 10:00 — Иван П., ул. Ленина 5 — уборка
• 14:30 — Мария С., пр. Мира 12 — мойка окон
• 18:00 — нет записей

━━━━━━━━━━━━━━━
```

Кнопки (2×2 + 1):
```
[📦 Заказы]    [👥 Клиенты]
[📢 Маркетинг] [📊 Отчёты]
      [⚙️ Настройки]
```

Команда `/home` или `/start` всегда возвращает на Home.

### 4.3 Блок «Заказы»

**Статусы заказа:**
`new` → `confirmed` → `done`  
`confirmed` → `moved` (перенос, создаётся новый заказ)  
`confirmed` → `cancelled`

**Создать заказ:**
1. Поиск клиента (текст → поиск по имени и телефону) или «+ Новый клиент»
2. Адрес — предлагается последний адрес клиента, можно изменить или ввести новый
3. Дата и время записи
4. Услуги — выбор из справочника (inline-кнопки) + «Написать свою»; можно добавить несколько
5. Итог — показывается список услуг и сумма, подтверждение
6. → Уведомление клиенту + событие в Google Calendar

**Список заказов:**
Фильтры: сегодня / завтра / неделя / все активные  
Карточка заказа: клиент, адрес, время, услуги, статус

**Провести заказ (статус → done):**
1. Итоговая сумма (предзаполнена суммой из заказа, можно изменить)
2. Тип оплаты: нал / карта / перевод / по счёту
3. Списать бонусы? (если есть баланс) — показать баланс, ввести сумму списания
4. Подтверждение → начислить бонусы автоматически (если включено у мастера)
5. → Уведомление клиенту: «Заказ выполнен, начислено N бонусов»

**Перенести заказ:**
1. Новая дата и время
2. → Обновить событие в GC + уведомление клиенту

**Отменить заказ:**
1. Причина (текст, необязательно)
2. → Удалить событие из GC + уведомление клиенту

### 4.4 Блок «Клиенты»

**Поиск:** единая строка → ищет по имени и телефону одновременно

**Добавить клиента вручную:**
Имя → телефон → дата рождения (необязательно, можно пропустить)

**Карточка клиента:**
```
👤 Иван Петров
📞 +7 999 123-45-67
🎂 15 марта
💰 Бонусов: 450 ₽
🛒 Заказов: 8 | Потрачено: 24 500 ₽
📝 Заметка: аллергия на хлорку

[📋 История]  [🎁 Бонусы]
[✏️ Изменить] [📝 Заметка]
```

**История заказов клиента:** список с датой, суммой, услугами, статусом

**Лог бонусов клиента:** все операции начисления/списания с датой и комментарием

**Ручное начисление/списание бонусов:**
Сумма → комментарий → подтверждение

**Редактировать данные:** имя, телефон, ДР по отдельности

### 4.5 Блок «Маркетинг»

**Рассылка:**
1. Текст сообщения
2. Сегмент получателей:
   - Все клиенты
   - Не приходили 3+ месяца
   - Не приходили 6+ месяцев
   - Новые за последние 30 дней
3. Предпросмотр + количество получателей
4. Подтверждение → отправка через client_bot

**Акция:**
1. Название акции
2. Описание (что за акция, условия)
3. Период действия (с... по...)
4. Подтверждение → сохранить + разослать уведомление всем клиентам

Активные акции отображаются в client_bot в разделе «Акции».

### 4.6 Блок «Отчёты»

Периоды: сегодня / неделя / текущий месяц / произвольный период

Показатели:
- Выручка общая
- Выручка по типам услуг (топ-5 услуг по сумме)
- Выручка по типу оплаты (нал / карта / перевод / по счёту)
- Количество заказов
- Новых клиентов за период
- Повторных клиентов за период
- Средний чек
- Всего клиентов в базе

### 4.7 Блок «Настройки»

**Профиль:**
- Имя / псевдоним
- Сфера деятельности
- Соцсети и каналы
- Контакты
- Режим работы

**Бонусная программа:**
- Вкл/выкл программу лояльности
- % начисления бонусов (по умолчанию 5%)
- Макс. % суммы заказа, оплачиваемый бонусами (по умолчанию 50%)
- Бонус на день рождения (по умолчанию 300 ₽)

**Справочник услуг:**
- Список услуг с ценами
- Добавить: название + цена
- Редактировать / деактивировать / удалить

**Инвайт-ссылка:**
- Показать ссылку: `t.me/{client_bot_username}?start={invite_token}`
- Показать QR-код (генерируется как изображение)

---

## 5. client_bot — Функциональные требования

### 5.1 Регистрация клиента

Триггер: `/start {invite_token}` — переход по инвайт-ссылке мастера  
Триггер: `/start` без токена — сообщение «Для регистрации нужна ссылка от вашего мастера»

Сценарий (анкета, FSM):
1. Как вас зовут?
2. Ваш номер телефона (кнопка «Поделиться контактом» или ввод вручную)
3. Дата рождения (необязательно, кнопка «Пропустить»)

После регистрации: клиент привязывается к мастеру через `master_clients`, показывается Home экран.

Если клиент уже есть в базе (добавлен мастером вручную по телефону) — привязывается tg_id к существующей записи.

### 5.2 Home экран клиента

```
👋 Привет, [Имя]!

Ваш мастер: [Имя мастера]
💰 Бонусов: 450 ₽
```

Кнопки:
```
[💰 Мои бонусы]  [📋 История]
[🎁 Акции]       [📞 Заказать]
[❓ Вопрос]      [📸 Фото/видео]
     [🔔 Уведомления]
```

### 5.3 Разделы client_bot

**Мои бонусы:** текущий баланс + лог последних 10 операций

**История:** список выполненных заказов (дата, услуги, сумма, начислено бонусов)

**Акции:** список активных акций мастера (название, описание, до какого числа)

**Заказать (заявка):**
1. Выбор услуги из справочника мастера (или «Другое»)
2. Комментарий (необязательно)
3. → Мастеру уведомление: «🛎 Новая заявка на заказ» + имя, телефон, tg, услуга, комментарий

**Вопрос:**
1. Ввести текст вопроса
2. → Мастеру уведомление: «❓ Вопрос от клиента» + имя, телефон, tg, текст

**Фото/видео:**
1. Отправить медиафайл
2. Комментарий (необязательно)
3. → Мастеру уведомление с медиа + подпись: кто прислал, телефон, комментарий

**Настройки уведомлений:**
- Напоминание за 24ч — вкл/выкл (по умолчанию вкл)
- Напоминание за 1ч — вкл/выкл (по умолчанию вкл)
- Маркетинговые рассылки — вкл/выкл (по умолчанию вкл)
- Акции — вкл/выкл (по умолчанию вкл)

*Уведомления о статусах заказа (создан, перенесён, отменён, выполнен) — всегда включены, не отключаются.*

---

## 6. Автоматические уведомления

| Событие | Кому | Содержание |
|---|---|---|
| Заказ создан | Клиенту | Дата, время, адрес, услуги, контакт мастера |
| Заказ перенесён | Клиенту | Новая дата и время |
| Заказ отменён | Клиенту | Причина (если указана) |
| Заказ выполнен | Клиенту | Сумма, начислено бонусов, текущий баланс |
| За 24ч до заказа | Клиенту | Дата, время, адрес + кнопка «Подтверждаю» |
| Подтверждение от клиента | Мастеру | Имя клиента, заказ |
| За 1ч до заказа | Клиенту | Время, адрес, контакты мастера + кнопка «Написать мастеру» |
| День рождения | Клиенту | Поздравление + начислен бонус (если включено) |
| Вопрос / заявка / медиа | Мастеру | Тип запроса + данные клиента + контент |

---

## 7. Интеграция с Google Calendar

**Направление:** только бот → GC (односторонняя)

**Аутентификация:** OAuth2 (мастер авторизует доступ к своему календарю через настройки)  
*В MVP: сервисный аккаунт или упрощённый OAuth flow — уточнить при реализации*

**Операции:**

| Действие в боте | Действие в GC |
|---|---|
| Создать заказ | Создать событие (название: «[Клиент] — [Услуга]», описание: адрес, телефон) |
| Перенести заказ | Обновить время события |
| Отменить заказ | Удалить событие |

**Поле `gc_event_id`** в таблице `orders` хранит ID события для обновления/удаления.

---

## 8. Требования к безопасности

### 8.1 Изоляция данных между мастерами

- Каждый запрос к БД обязательно включает `master_id` из проверенного контекста (tg_id мастера)
- Клиент видит только данные своего мастера (фильтрация по `master_id` через `master_clients`)
- Справочник услуг, заказы, кампании — всегда с проверкой `master_id`

### 8.2 Валидация входных данных

- Телефон: только цифры, российский формат (+7/8/9...)
- Дата: валидация формата перед записью в БД
- Суммы: только положительные числа, целые (рубли)
- Процент бонусов: 0–100
- Текстовые поля: обрезка пробелов, ограничение длины (имя ≤ 100, заметка ≤ 500, описание ≤ 1000)

### 8.3 Защита invite_token

- Токен генерируется как `secrets.token_urlsafe(16)` — криптографически стойкий
- Один токен — один мастер
- Токен не отображается в логах

### 8.4 Rate limiting

- Рассылки: не более 30 сообщений в секунду (ограничение Telegram Bot API)
- Задержка между сообщениями в рассылке: не менее 50 мс

### 8.5 Обработка ошибок

- Все обращения к внешним API (Telegram, Google Calendar) — в try/except с логированием
- FSM-состояния очищаются при ошибке и по команде `/cancel`
- Пользователь всегда получает понятное сообщение об ошибке (без технических деталей)

### 8.6 Переменные окружения

Все чувствительные данные — только в `.env`, никогда в коде:
```
MASTER_BOT_TOKEN=
CLIENT_BOT_TOKEN=
DATABASE_URL=sqlite:///db.sqlite3
GOOGLE_CREDENTIALS_PATH=
CLIENT_BOT_USERNAME=
```

---

## 9. Нефункциональные требования

| Параметр | Требование |
|---|---|
| Язык | Python 3.11+ |
| Фреймворк | aiogram 3.x |
| БД (dev) | SQLite + aiosqlite |
| БД (prod) | PostgreSQL + asyncpg |
| Планировщик | APScheduler 3.x |
| Хранение состояний | aiogram FSM (MemoryStorage dev, RedisStorage prod) |
| Логирование | Python logging → файл + консоль |
| Деплой | Systemd-сервисы на VPS (два отдельных процесса) |

---

## 10. Этапы разработки MVP

| Этап | Содержание |
|---|---|
| **1. Фундамент** | Структура проекта, схема БД, миграции, конфиг, регистрация мастера, инвайт и регистрация клиента |
| **2. Заказы** | CRUD заказов, статусы, провести заказ, интеграция с GC |
| **3. Клиенты и бонусы** | Карточка клиента, лог бонусов, начисление/списание, бонус на ДР |
| **4. Уведомления** | Все авто-уведомления, напоминания за 24ч и 1ч (планировщик) |
| **5. Маркетинг** | Рассылки с сегментацией, акции |
| **6. Отчёты и настройки** | Все отчёты, справочник услуг, QR-код инвайта |
| **7. Прод** | PostgreSQL, деплой на VPS, RedisStorage, нагрузочное тестирование |

---

## 11. Вне скопа MVP (v2+)

- Планировщик рассылок (отложенная отправка)
- Двусторонняя синхронизация с Google Calendar
- Мини-чат мастер ↔ клиент внутри бота
- Монетизация (подписка / freemium)
- Аналитика по клиентам (LTV, retention)
- Экспорт базы клиентов в CSV
- Поддержка нескольких мастеров на один аккаунт (для малого бизнеса)
