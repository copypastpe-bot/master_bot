# Allowed browser origins for Mini App API
map $http_origin $cors_origin {
    default "";
    "https://app.abooking.org" $http_origin;
}

# Mini App frontend — app.abooking.org
server {
    listen 80;
    listen [::]:80;
    server_name app.abooking.org;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name app.abooking.org;

    ssl_certificate /etc/letsencrypt/live/app.abooking.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/app.abooking.org/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    root /var/www/app.abooking.org;
    index index.html;

    # SPA routing — all routes to index.html
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Cache static assets 1 year
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

# FastAPI backend — api.abooking.org
server {
    listen 80;
    listen [::]:80;
    server_name api.abooking.org;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name api.abooking.org;

    ssl_certificate /etc/letsencrypt/live/api.abooking.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.abooking.org/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
    client_max_body_size 60M;

    location /media/promo/ {
        proxy_pass http://127.0.0.1:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        expires 7d;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*" always;
    }

    location / {
        proxy_pass http://127.0.0.1:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Credentials;

        add_header Access-Control-Allow-Origin $cors_origin always;
        add_header Access-Control-Allow-Credentials "true" always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type, X-Init-Data" always;
        add_header Vary "Origin" always;

        # Handle CORS preflight
        if ($request_method = OPTIONS) {
            return 204;
        }
    }
}
